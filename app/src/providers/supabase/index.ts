import insert from "./database/property/insert";
import strapiPropertyExists from "./database/property/strapiPropertyExists";

export default { database: { property: { strapiPropertyExists, insert } } };
